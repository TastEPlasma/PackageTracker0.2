<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:PackageTracker" mc:Ignorable="d" x:Class="PackageTracker.MainWindow"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Window.Resources>
        <CollectionViewSource x:Key="trackerContextViewSource" d:DesignSource="{d:DesignInstance {x:Type local:TrackerContext}, CreateList=True}"/>
        <CollectionViewSource x:Key="trackerContextPackagesViewSource" Source="{Binding Packages, Source={StaticResource trackerContextViewSource}}"/>
        <CollectionViewSource x:Key="trackerDataViewSource" d:DesignSource="{d:DesignInstance {x:Type local:TrackerData}, CreateList=True}"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource trackerContextPackagesViewSource}">
        <Button x:Name="Update" Content="Update" HorizontalAlignment="Left" Margin="220,235,0,0" VerticalAlignment="Top" Width="75" Click="Update_Click"/>
        <DataGrid x:Name="trackerDataDataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" ItemsSource="{Binding Source={StaticResource trackerDataViewSource}}" Margin="10,10,10,109" RowDetailsVisibilityMode="VisibleWhenSelected" Background="White">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn x:Name="deleteme" Binding="{Binding DeleteMe}" Header="Delete" Width="50"/>
                <DataGridTextColumn x:Name="trackingNumberColumn" Binding="{Binding TrackingNumber}" Header="Tracking Number" Width="150"/>
                <DataGridTextColumn x:Name="locationColumn" Binding="{Binding Location}" Header="Location" Width="300"/>                
            </DataGrid.Columns>
        </DataGrid>
        <ProgressBar Name ="Progress" Visibility="Hidden" IsIndeterminate="True" HorizontalAlignment="Left" Height="10" Margin="207,283,0,0" VerticalAlignment="Top" Width="100"/>

    </Grid>
</Window>
